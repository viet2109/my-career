import React from 'react';
import PropTypes from 'prop-types';

SignIn.propTypes = {
    
};

function SignIn(props) {
    return (
        <div>
            SignIn
        </div>
    );
}

export default SignIn;