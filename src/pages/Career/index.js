import React from 'react';
import PropTypes from 'prop-types';

Career.propTypes = {
    
};

function Career(props) {
    return (
        <div>
            Career
        </div>
    );
}

export default Career;