import React from 'react';
import PropTypes from 'prop-types';

SignUp.propTypes = {
    
};

function SignUp(props) {
    return (
        <div>
            
        </div>
    );
}

export default SignUp;